# Frontend (Expo Web)
Project-specific quick start. See root README for overall setup.

## Run locally
```

npm run web
```

## API endpoint
By default the app uses the current host (`window.location.hostname`). Override via env if needed.

## Docs
- Root overview: ../README.md
- Architecture: ../Docs/architecture.md
- Products/pricing API: ../SISTEMA_PRODUCTOS_PRECIOS.md
   ```
